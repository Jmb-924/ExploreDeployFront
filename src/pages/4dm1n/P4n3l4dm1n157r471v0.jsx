import PropTypes from 'prop-types'

const PanelAdministrativo = ({ children }) => {
   return (
      <>Panel del Admin</>
   )
}

PanelAdministrativo.propTypes = {
   children: PropTypes.node
}

export default PanelAdministrativo